spring:
  application:
    name: config-Dev

eureka:
  client:
    registry-with-eureka: 'true'
    service-url:
      defaultZone: http://localhost:8080/eureka
    fetch-registry: 'true'
  instance:
    prefer-ip-address: 'true'
